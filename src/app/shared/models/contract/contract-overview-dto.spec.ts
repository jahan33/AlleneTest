import { ContractOverviewDto } from './contract-overview-dto';

describe('ContractOverviewDto', () => {
  it('should create an instance', () => {
    expect(new ContractOverviewDto()).toBeTruthy();
  });
});
